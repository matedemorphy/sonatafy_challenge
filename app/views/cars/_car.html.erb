<tr>
	<td><%= car.make %></td>
	<td><%= car.model %></td>
	<td><%= car.condition %></td>
	<td><%= number_to_currency(car.price) %></td>
	<td><%= car.dealership.location %></td>
	<% if current_user.admin? %>
		<td><%= delete_confirm(car) %></td>
	<% end %>
</tr>